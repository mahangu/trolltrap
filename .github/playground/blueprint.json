{
  "$schema": "https://playground.wordpress.net/blueprint-schema.json",
  "login": true,
  "steps": [
    {
      "step": "activatePlugin",
      "pluginPath": "/wordpress/wp-content/plugins/troll-trap/index.php"
    },
    {
      "step": "setSiteOptions",
      "options": {
        "trolltrap_words": "badger\nmushroom",
        "trolltrap_default_filter": "disemvowel"
      }
    },
    {
      "step": "runPHP",
      "code": "<?php require_once '/wordpress/wp-load.php'; if ( ! class_exists( 'Mahangu_Troll_Trap' ) ) { fwrite( STDERR, 'FAIL: main class not loaded' . PHP_EOL ); exit( 1 ); } if ( get_option( 'trolltrap_default_filter' ) !== 'disemvowel' ) { fwrite( STDERR, 'FAIL: option not stored' . PHP_EOL ); exit( 1 ); } echo 'OK: plugin loads, options round-trip' . PHP_EOL;"
    },
    {
      "step": "runPHP",
      "code": "<?php require_once '/wordpress/wp-load.php'; wp_set_current_user( 1 ); $bulk = apply_filters( 'bulk_actions-edit-comments', array() ); if ( ! isset( $bulk['mark_as_troll'] ) ) { fwrite( STDERR, 'FAIL: mark_as_troll bulk action missing' . PHP_EOL ); exit( 1 ); } if ( ! isset( $bulk['untrap'] ) ) { fwrite( STDERR, 'FAIL: untrap bulk action missing' . PHP_EOL ); exit( 1 ); } echo 'OK: both bulk actions registered' . PHP_EOL;"
    },
    {
      "step": "runPHP",
      "code": "<?php require_once '/wordpress/wp-load.php'; wp_set_current_user( 1 ); $cid = wp_insert_comment( array( 'comment_post_ID' => 1, 'comment_content' => 'untrap target', 'comment_author' => 'tester', 'comment_author_email' => 'tester@example.com' ) ); if ( ! $cid ) { fwrite( STDERR, 'FAIL: could not create test comment' . PHP_EOL ); exit( 1 ); } update_comment_meta( $cid, '_trolltrap_filter', 'piglatin' ); $_REQUEST['_wpnonce'] = wp_create_nonce( 'bulk-comments' ); $tt = mahangu_troll_trap(); $tt->settings->handle_bulk_actions( '', 'untrap', array( $cid ) ); $result = get_comment_meta( $cid, '_trolltrap_filter', true ); if ( $result !== 'none' ) { fwrite( STDERR, 'FAIL: untrap did not clear filter, got: ' . var_export( $result, true ) . PHP_EOL ); exit( 1 ); } echo 'OK: untrap clears _trolltrap_filter to none' . PHP_EOL;"
    },
    {
      "step": "runPHP",
      "code": "<?php require_once '/wordpress/wp-load.php'; wp_set_current_user( 1 ); $cid = wp_insert_comment( array( 'comment_post_ID' => 1, 'comment_content' => 'mark target', 'comment_author' => 'tester2', 'comment_author_email' => 'tester2@example.com' ) ); update_comment_meta( $cid, '_trolltrap_filter', 'none' ); $_REQUEST['_wpnonce'] = wp_create_nonce( 'bulk-comments' ); $tt = mahangu_troll_trap(); $tt->settings->handle_bulk_actions( '', 'mark_as_troll', array( $cid ) ); $result = get_comment_meta( $cid, '_trolltrap_filter', true ); if ( $result !== 'disemvowel' ) { fwrite( STDERR, 'FAIL: mark_as_troll did not set default filter (disemvowel), got: ' . var_export( $result, true ) . PHP_EOL ); exit( 1 ); } echo 'OK: mark_as_troll still applies default filter (regression check)' . PHP_EOL;"
    },
    {
      "step": "runPHP",
      "code": "<?php require_once '/wordpress/wp-load.php'; wp_set_current_user( 1 ); $tt = mahangu_troll_trap(); $_REQUEST = array( 'bulk_untrap_comments' => 2 ); ob_start(); $tt->settings->handle_bulk_actions_notice(); $out = ob_get_clean(); if ( strpos( $out, 'Untrapped 2 comments.' ) === false ) { fwrite( STDERR, 'FAIL: untrap notice text missing or wrong. Got: ' . $out . PHP_EOL ); exit( 1 ); } $_REQUEST = array( 'bulk_troll_comments' => 1 ); ob_start(); $tt->settings->handle_bulk_actions_notice(); $out = ob_get_clean(); if ( strpos( $out, 'Marked 1 comment as Troll.' ) === false ) { fwrite( STDERR, 'FAIL: mark notice text missing or wrong. Got: ' . $out . PHP_EOL ); exit( 1 ); } echo 'OK: bulk action notices render correctly (singular + plural)' . PHP_EOL;"
    },
    {
      "step": "runPHP",
      "code": "<?php require_once '/wordpress/wp-load.php'; $c = mahangu_troll_trap()->convert; $rev = $c->reverse( 'héllo wörld' ); if ( ! mb_check_encoding( $rev, 'UTF-8' ) ) { fwrite( STDERR, 'FAIL: reverse produced invalid UTF-8' . PHP_EOL ); exit( 1 ); } if ( $rev !== 'olléh dlröw' ) { fwrite( STDERR, 'FAIL: multibyte reverse wrong, got: ' . $rev . PHP_EOL ); exit( 1 ); } $multi = $c->pig_latin( 'line one' . PHP_EOL . 'line two' ); if ( strpos( $multi, PHP_EOL ) === false ) { fwrite( STDERR, 'FAIL: pig_latin collapsed the newline' . PHP_EOL ); exit( 1 ); } echo 'OK: converters are multibyte- and whitespace-safe' . PHP_EOL;"
    },
    {
      "step": "runPHP",
      "code": "<?php require_once '/wordpress/wp-load.php'; $reg = mahangu_troll_trap()->filters; if ( ! ( $reg instanceof Mahangu_Troll_Trap_Filters ) ) { fwrite( STDERR, 'FAIL: filter registry not available' . PHP_EOL ); exit( 1 ); } if ( $reg->apply( 'disemvowel', 'Hello World' ) !== 'Hll Wrld' ) { fwrite( STDERR, 'FAIL: registry apply(disemvowel) wrong' . PHP_EOL ); exit( 1 ); } if ( $reg->apply( 'none', 'Hello World' ) !== 'Hello World' ) { fwrite( STDERR, 'FAIL: registry apply(none) should be identity' . PHP_EOL ); exit( 1 ); } if ( $reg->apply( 'bogus-slug', 'Hello World' ) !== 'Hello World' ) { fwrite( STDERR, 'FAIL: registry apply(unknown) should be identity' . PHP_EOL ); exit( 1 ); } if ( in_array( 'none', array_keys( $reg->transforming() ), true ) ) { fwrite( STDERR, 'FAIL: transforming() must exclude none' . PHP_EOL ); exit( 1 ); } if ( count( $reg->slugs() ) < 4 ) { fwrite( STDERR, 'FAIL: expected at least 4 registered filters' . PHP_EOL ); exit( 1 ); } echo 'OK: filter registry registered and dispatching' . PHP_EOL;"
    },
    {
      "step": "runPHP",
      "code": "<?php require_once '/wordpress/wp-load.php'; $reg = mahangu_troll_trap()->filters; foreach ( array( 'leetspeak', 'mocking', 'uwu', 'rot13', 'zalgo' ) as $slug ) { if ( ! $reg->has( $slug ) ) { fwrite( STDERR, 'FAIL: filter ' . $slug . ' not registered' . PHP_EOL ); exit( 1 ); } $out = $reg->apply( $slug, 'héllo Wörld 123' ); if ( ! mb_check_encoding( $out, 'UTF-8' ) ) { fwrite( STDERR, 'FAIL: ' . $slug . ' produced invalid UTF-8' . PHP_EOL ); exit( 1 ); } } if ( $reg->apply( 'rot13', $reg->apply( 'rot13', 'Hello' ) ) !== 'Hello' ) { fwrite( STDERR, 'FAIL: rot13 not self-inverse' . PHP_EOL ); exit( 1 ); } if ( $reg->apply( 'uwu', 'really' ) !== 'weawwy' ) { fwrite( STDERR, 'FAIL: uwu wrong' . PHP_EOL ); exit( 1 ); } echo 'OK: new transforms registered and producing valid UTF-8' . PHP_EOL;"
    },
    {
      "step": "runPHP",
      "code": "<?php require_once '/wordpress/wp-load.php'; update_option( 'trolltrap_words', 'alpha' . PHP_EOL . 'beta' . PHP_EOL . 'gamma' ); update_option( 'trolltrap_graduated_enabled', '1' ); update_option( 'trolltrap_severity_ladder', array( 1 => 'piglatin', 2 => 'reverse', 3 => 'disemvowel' ) ); $tt = mahangu_troll_trap(); $two = wp_insert_comment( array( 'comment_post_ID' => 1, 'comment_content' => 'alpha and beta here', 'comment_author' => 'grad2', 'comment_author_email' => 'grad2@example.com' ) ); $tt->comments_tag( $two ); if ( get_comment_meta( $two, '_trolltrap_match_count', true ) != 2 ) { fwrite( STDERR, 'FAIL: expected match count 2' . PHP_EOL ); exit( 1 ); } if ( get_comment_meta( $two, '_trolltrap_filter', true ) !== 'reverse' ) { fwrite( STDERR, 'FAIL: rung 2 should map to reverse' . PHP_EOL ); exit( 1 ); } $three = wp_insert_comment( array( 'comment_post_ID' => 1, 'comment_content' => 'alpha beta gamma', 'comment_author' => 'grad3', 'comment_author_email' => 'grad3@example.com' ) ); $tt->comments_tag( $three ); if ( get_comment_meta( $three, '_trolltrap_filter', true ) !== 'disemvowel' ) { fwrite( STDERR, 'FAIL: rung 3+ should map to disemvowel' . PHP_EOL ); exit( 1 ); } $zero = wp_insert_comment( array( 'comment_post_ID' => 1, 'comment_content' => 'a perfectly clean remark', 'comment_author' => 'grad0', 'comment_author_email' => 'grad0@example.com' ) ); $tt->comments_tag( $zero ); if ( get_comment_meta( $zero, '_trolltrap_filter', true ) !== 'none' ) { fwrite( STDERR, 'FAIL: no match should be none' . PHP_EOL ); exit( 1 ); } echo 'OK: graduated severity ladder maps match count to filter' . PHP_EOL;"
    },
    {
      "step": "runPHP",
      "code": "<?php require_once '/wordpress/wp-load.php'; $tt = mahangu_troll_trap(); if ( ! ( $tt->ai instanceof Mahangu_Troll_Trap_AI ) ) { fwrite( STDERR, 'FAIL: AI feature object missing' . PHP_EOL ); exit( 1 ); } if ( $tt->ai->is_available() ) { fwrite( STDERR, 'FAIL: AI should be unavailable with no key configured' . PHP_EOL ); exit( 1 ); } if ( in_array( 'llm', $tt->filters->slugs(), true ) ) { fwrite( STDERR, 'FAIL: llm filter should not register while AI is unavailable' . PHP_EOL ); exit( 1 ); } if ( '' === $tt->ai->fallback_slug() ) { fwrite( STDERR, 'FAIL: AI fallback slug should resolve to a filter' . PHP_EOL ); exit( 1 ); } echo 'OK: AI rewrite feature is gated off by default' . PHP_EOL;"
    },
    {
      "step": "runPHP",
      "code": "<?php require_once '/wordpress/wp-load.php'; update_option( 'trolltrap_words', 'badger' ); update_option( 'trolltrap_default_filter', 'disemvowel' ); update_option( 'trolltrap_graduated_enabled', '0' ); $tt = mahangu_troll_trap(); $cid = wp_insert_comment( array( 'comment_post_ID' => 1, 'comment_content' => 'badger nonsense', 'comment_author' => 'render-test', 'comment_author_email' => 'render-test@example.com' ) ); $tt->comments_tag( $cid ); $stored = get_comment_meta( $cid, '_trolltrap_filter', true ); if ( $stored !== 'disemvowel' ) { echo 'FAIL: keyword-matching did not set the disemvowel filter, got: ' . var_export( $stored, true ) . PHP_EOL; exit( 1 ); } $comment = get_comment( $cid ); $rendered = apply_filters( 'comment_text', $comment->comment_content, $comment ); if ( strpos( $rendered, 'bdgr nnsns' ) === false ) { echo 'FAIL: comment_text did not run through disemvowel, got: ' . var_export( $rendered, true ) . PHP_EOL; exit( 1 ); } if ( strpos( $rendered, 'badger' ) !== false ) { echo 'FAIL: original keyword leaked through comment_text, got: ' . var_export( $rendered, true ) . PHP_EOL; exit( 1 ); } echo 'OK: comment_text filter renders trapped comment through chosen transform' . PHP_EOL;"
    },
    {
      "step": "runPHP",
      "code": "<?php require_once '/wordpress/wp-load.php'; update_option( 'trolltrap_words', 'badger' ); update_option( 'trolltrap_default_filter', 'disemvowel' ); update_option( 'trolltrap_graduated_enabled', '0' ); wp_set_current_user( 1 ); $tt = mahangu_troll_trap(); $cid = wp_insert_comment( array( 'comment_post_ID' => 1, 'comment_content' => 'badger content', 'comment_author' => 'col-test', 'comment_author_email' => 'col-test@example.com' ) ); $tt->comments_tag( $cid ); ob_start(); $tt->settings->admin_column_output( 'trolltrap', $cid ); $out = ob_get_clean(); if ( strpos( $out, 'Matched graylist keywords' ) === false ) { echo 'FAIL: admin column missing matched-keywords caption. Got: ' . $out . PHP_EOL; exit( 1 ); } if ( strpos( $out, 'badger' ) === false ) { echo 'FAIL: admin column missing the actual matched word. Got: ' . $out . PHP_EOL; exit( 1 ); } if ( strpos( $out, 'name=\"trolltrap\"' ) === false ) { echo 'FAIL: admin column missing the filter select. Got: ' . $out . PHP_EOL; exit( 1 ); } echo 'OK: admin column renders the filter dropdown and matched-keywords caption' . PHP_EOL;"
    },
    {
      "step": "runPHP",
      "code": "<?php require_once '/wordpress/wp-load.php'; $links = apply_filters( 'plugin_action_links_troll-trap/index.php', array( '<a href=\"#\">Deactivate</a>' ) ); if ( ! is_array( $links ) || count( $links ) < 2 ) { echo 'FAIL: plugin_action_links did not return the prepended link. Got: ' . var_export( $links, true ) . PHP_EOL; exit( 1 ); } if ( strpos( $links[0], 'options-discussion.php' ) === false ) { echo 'FAIL: Settings link does not target Settings > Discussion. Got: ' . $links[0] . PHP_EOL; exit( 1 ); } if ( strpos( $links[0], '>Settings</a>' ) === false ) { echo 'FAIL: Settings link label missing. Got: ' . $links[0] . PHP_EOL; exit( 1 ); } echo 'OK: plugin_action_links prepends a Settings shortcut to Discussion' . PHP_EOL;"
    },
    {
      "step": "runPHP",
      "code": "<?php require_once '/wordpress/wp-load.php'; wp_set_current_user( 1 ); update_option( 'trolltrap_words', '' ); update_option( 'trolltrap_default_filter', 'disemvowel' ); update_option( 'trolltrap_graduated_enabled', '0' ); $tt = mahangu_troll_trap(); $cid = wp_insert_comment( array( 'comment_post_ID' => 1, 'comment_content' => 'badger lives here', 'comment_author' => 'reeval', 'comment_author_email' => 'reeval@example.com' ) ); $tt->comments_tag( $cid ); if ( get_comment_meta( $cid, '_trolltrap_filter', true ) !== 'none' ) { echo 'FAIL: empty graylist should yield none filter, got: ' . var_export( get_comment_meta( $cid, '_trolltrap_filter', true ), true ) . PHP_EOL; exit( 1 ); } $bulk = apply_filters( 'bulk_actions-edit-comments', array() ); if ( ! isset( $bulk['reevaluate'] ) ) { echo 'FAIL: reevaluate bulk action not registered' . PHP_EOL; exit( 1 ); } update_option( 'trolltrap_words', 'badger' ); $_REQUEST['_wpnonce'] = wp_create_nonce( 'bulk-comments' ); $tt->settings->handle_bulk_actions( '', 'reevaluate', array( $cid ) ); $after = get_comment_meta( $cid, '_trolltrap_filter', true ); if ( $after !== 'disemvowel' ) { echo 'FAIL: re-evaluate did not apply the now-matching filter, got: ' . var_export( $after, true ) . PHP_EOL; exit( 1 ); } if ( (int) get_comment_meta( $cid, '_trolltrap_match_count', true ) !== 1 ) { echo 'FAIL: re-evaluate did not update match count' . PHP_EOL; exit( 1 ); } echo 'OK: re-evaluate bulk action re-runs comments_tag against the current graylist' . PHP_EOL;"
    },
    {
      "step": "runPHP",
      "code": "<?php require_once '/wordpress/wp-load.php'; require_once '/wordpress/wp-content/plugins/troll-trap/includes/class-trolltrap-cli.php'; if ( ! class_exists( 'Mahangu_Troll_Trap_CLI' ) ) { echo 'FAIL: CLI class not defined after require' . PHP_EOL; exit( 1 ); } $ref = new ReflectionClass( 'Mahangu_Troll_Trap_CLI' ); foreach ( array( 'mark', 'untrap', 'reevaluate', 'regenerate_ai', 'status', 'filters', 'dry_run_graylist', 'dry_run_allowlist' ) as $method ) { if ( ! $ref->hasMethod( $method ) ) { echo 'FAIL: CLI missing expected command method: ' . $method . PHP_EOL; exit( 1 ); } $m = $ref->getMethod( $method ); if ( ! $m->isPublic() ) { echo 'FAIL: CLI method should be public: ' . $method . PHP_EOL; exit( 1 ); } } echo 'OK: CLI class loads with mark, untrap, reevaluate, status, filters commands' . PHP_EOL;"
    },
    {
      "step": "runPHP",
      "code": "<?php require_once '/wordpress/wp-load.php'; $tt = mahangu_troll_trap(); $cid = wp_insert_comment( array( 'comment_post_ID' => 1, 'comment_content' => 'ai regen target', 'comment_author' => 'regen', 'comment_author_email' => 'regen@example.com' ) ); update_comment_meta( $cid, '_trolltrap_filter', 'llm' ); update_comment_meta( $cid, '_trolltrap_llm_text', 'a stale rewrite from a prior run' ); update_comment_meta( $cid, '_trolltrap_llm_attempts', 2 ); if ( null === $tt->ai->cached_text( $cid ) ) { echo 'FAIL: precondition failed, cached_text returned null before regenerate' . PHP_EOL; exit( 1 ); } $tt->ai->regenerate( $cid ); $remaining = get_comment_meta( $cid, '_trolltrap_llm_text', true ); if ( '' !== $remaining ) { echo 'FAIL: regenerate did not clear the cached rewrite, got: ' . var_export( $remaining, true ) . PHP_EOL; exit( 1 ); } if ( '' !== get_comment_meta( $cid, '_trolltrap_llm_attempts', true ) ) { echo 'FAIL: regenerate did not reset the attempts counter' . PHP_EOL; exit( 1 ); } if ( null !== $tt->ai->cached_text( $cid ) ) { echo 'FAIL: cached_text still returns a value after regenerate' . PHP_EOL; exit( 1 ); } if ( false === $tt->ai->regenerate( 0 ) ) { echo 'OK: regenerate clears the cached rewrite and resets attempts and rejects bogus IDs' . PHP_EOL; } else { echo 'FAIL: regenerate should return false for invalid comment ID' . PHP_EOL; exit( 1 ); }"
    },
    {
      "step": "runPHP",
      "code": "<?php require_once '/wordpress/wp-load.php'; update_option( 'trolltrap_ai_enabled', '1' ); update_option( 'trolltrap_ai_key', 'sk-ant-test-fake' ); $tt = mahangu_troll_trap(); if ( ! $tt->ai->is_available() ) { echo 'FAIL: AI should be available with key + enabled' . PHP_EOL; exit( 1 ); } $cid = wp_insert_comment( array( 'comment_post_ID' => 1, 'comment_content' => 'retry me please', 'comment_author' => 'retry', 'comment_author_email' => 'retry@example.com' ) ); update_comment_meta( $cid, '_trolltrap_filter', 'llm' ); $call_count = 0; add_filter( 'pre_http_request', function( $pre, $args, $url ) use ( &$call_count ) { if ( strpos( $url, 'api.anthropic.com' ) === false ) { return $pre; } $call_count++; if ( 1 === $call_count ) { return array( 'response' => array( 'code' => 503, 'message' => 'Overloaded' ), 'body' => '', 'headers' => array(), 'cookies' => array(), 'filename' => null ); } return array( 'response' => array( 'code' => 200, 'message' => 'OK' ), 'body' => wp_json_encode( array( 'content' => array( array( 'type' => 'text', 'text' => 'a polished rewrite' ) ) ) ), 'headers' => array(), 'cookies' => array(), 'filename' => null ); }, 10, 3 ); $tt->ai->run_transform( $cid ); if ( 1 !== (int) get_comment_meta( $cid, '_trolltrap_llm_attempts', true ) ) { echo 'FAIL: first transient failure should leave attempts=1, got: ' . var_export( get_comment_meta( $cid, '_trolltrap_llm_attempts', true ), true ) . PHP_EOL; exit( 1 ); } if ( '' !== get_comment_meta( $cid, '_trolltrap_llm_text', true ) ) { echo 'FAIL: transient failure should not cache any text' . PHP_EOL; exit( 1 ); } $tt->ai->run_transform( $cid ); if ( 'a polished rewrite' !== get_comment_meta( $cid, '_trolltrap_llm_text', true ) ) { echo 'FAIL: second call should cache the successful rewrite, got: ' . var_export( get_comment_meta( $cid, '_trolltrap_llm_text', true ), true ) . PHP_EOL; exit( 1 ); } if ( '' !== get_comment_meta( $cid, '_trolltrap_llm_attempts', true ) ) { echo 'FAIL: successful rewrite should clear the attempts counter, got: ' . var_export( get_comment_meta( $cid, '_trolltrap_llm_attempts', true ), true ) . PHP_EOL; exit( 1 ); } echo 'OK: run_transform retries a 503 then caches on success' . PHP_EOL;"
    },
    {
      "step": "runPHP",
      "code": "<?php require_once '/wordpress/wp-load.php'; $tt = mahangu_troll_trap(); $cid = wp_insert_comment( array( 'comment_post_ID' => 1, 'comment_content' => 'failed sample', 'comment_author' => 'failtest', 'comment_author_email' => 'failtest@example.com' ) ); update_comment_meta( $cid, '_trolltrap_filter', 'llm' ); update_comment_meta( $cid, '_trolltrap_llm_attempts', 3 ); if ( ! $tt->ai->has_failed( $cid ) ) { echo 'FAIL: has_failed should be true with attempts=3 and no cache' . PHP_EOL; exit( 1 ); } update_comment_meta( $cid, '_trolltrap_llm_text', 'a cached rewrite' ); if ( $tt->ai->has_failed( $cid ) ) { echo 'FAIL: has_failed should be false when cached text exists, even with attempts=3' . PHP_EOL; exit( 1 ); } delete_comment_meta( $cid, '_trolltrap_llm_text' ); update_comment_meta( $cid, '_trolltrap_llm_attempts', 2 ); if ( $tt->ai->has_failed( $cid ) ) { echo 'FAIL: has_failed should be false with attempts below the cap' . PHP_EOL; exit( 1 ); } echo 'OK: has_failed flips only when attempts hit the cap and no cache exists' . PHP_EOL;"
    },
    {
      "step": "runPHP",
      "code": "<?php require_once '/wordpress/wp-load.php'; update_option( 'trolltrap_ai_enabled', '0' ); $tt = mahangu_troll_trap(); $r = $tt->ai->test_rewrite(); if ( 'unavailable' !== $r['status'] ) { echo 'FAIL: test_rewrite should report unavailable when AI is off, got: ' . var_export( $r, true ) . PHP_EOL; exit( 1 ); } update_option( 'trolltrap_ai_enabled', '1' ); update_option( 'trolltrap_ai_key', 'sk-ant-test-fake' ); add_filter( 'pre_http_request', function( $pre, $args, $url ) { if ( strpos( $url, 'api.anthropic.com' ) === false ) { return $pre; } return array( 'response' => array( 'code' => 200, 'message' => 'OK' ), 'body' => wp_json_encode( array( 'content' => array( array( 'type' => 'text', 'text' => 'Verily, thy comment is most discourteous.' ) ) ) ), 'headers' => array(), 'cookies' => array(), 'filename' => null ); }, 10, 3 ); $r = $tt->ai->test_rewrite(); if ( 'ok' !== $r['status'] || 'Verily, thy comment is most discourteous.' !== $r['text'] ) { echo 'FAIL: test_rewrite should return the mocked text on 200, got: ' . var_export( $r, true ) . PHP_EOL; exit( 1 ); } $r = $tt->ai->test_rewrite( '' ); if ( 'ok' !== $r['status'] ) { echo 'FAIL: empty string sample should fall back to the default sample, got: ' . var_export( $r, true ) . PHP_EOL; exit( 1 ); } $r = $tt->ai->test_rewrite( '   ' ); if ( 'ok' !== $r['status'] ) { echo 'FAIL: whitespace-only sample should fall back to the default sample' . PHP_EOL; exit( 1 ); } echo 'OK: test_rewrite routes through saved settings, reports unavailable / ok, and falls back to default sample on empty input' . PHP_EOL;"
    },
    {
      "step": "runPHP",
      "code": "<?php require_once '/wordpress/wp-load.php'; update_option( 'trolltrap_ai_enabled', '1' ); update_option( 'trolltrap_ai_key', 'sk-ant-test-fake' ); $tt = mahangu_troll_trap(); $cid = wp_insert_comment( array( 'comment_post_ID' => 1, 'comment_content' => 'retry-after test', 'comment_author' => 'ra', 'comment_author_email' => 'ra@example.com' ) ); update_comment_meta( $cid, '_trolltrap_filter', 'llm' ); wp_clear_scheduled_hook( 'trolltrap_ai_transform', array( $cid ) ); add_filter( 'pre_http_request', function( $pre, $args, $url ) { if ( strpos( $url, 'api.anthropic.com' ) === false ) { return $pre; } $h = new WpOrg\\Requests\\Response\\Headers(); $h['retry-after'] = '600'; return array( 'response' => array( 'code' => 429, 'message' => 'Too Many Requests' ), 'body' => '', 'headers' => $h, 'cookies' => array(), 'filename' => null ); }, 10, 3 ); $before = time(); $tt->ai->run_transform( $cid ); $next = wp_next_scheduled( 'trolltrap_ai_transform', array( $cid ) ); if ( ! $next ) { echo 'FAIL: cron event was not rescheduled after a 429 with Retry-After' . PHP_EOL; exit( 1 ); } $delay = $next - $before; if ( $delay < 500 ) { echo 'FAIL: scheduled delay should honor Retry-After (>= 500s), got: ' . $delay . 's' . PHP_EOL; exit( 1 ); } if ( 1 !== (int) get_comment_meta( $cid, '_trolltrap_llm_attempts', true ) ) { echo 'FAIL: transient failure should leave attempts=1' . PHP_EOL; exit( 1 ); } echo 'OK: 429 with Retry-After=600 schedules the retry at least ~600s out' . PHP_EOL;"
    },
    {
      "step": "runPHP",
      "code": "<?php require_once '/wordpress/wp-load.php'; $cid = wp_insert_comment( array( 'comment_post_ID' => 1, 'comment_content' => 'a comment mentioning badger', 'comment_author' => 'matcher', 'comment_author_email' => 'matcher@example.com' ) ); $c = get_comment( $cid ); $hit = Mahangu_Troll_Trap::match_keywords( $c, array( 'badger', 'absent' ) ); if ( array( 'badger' ) !== $hit ) { echo 'FAIL: match_keywords should return only the matching keyword, got: ' . var_export( $hit, true ) . PHP_EOL; exit( 1 ); } $hit = Mahangu_Troll_Trap::match_keywords( $c, array( 'absent', 'missing' ) ); if ( array() !== $hit ) { echo 'FAIL: match_keywords should return empty array for no matches' . PHP_EOL; exit( 1 ); } $hit = Mahangu_Troll_Trap::match_keywords( $c, array( '', '   ', 'badger' ) ); if ( array( 'badger' ) !== $hit ) { echo 'FAIL: match_keywords should ignore empty / whitespace entries' . PHP_EOL; exit( 1 ); } update_option( 'trolltrap_words', \"alpha\\nbeta\\n\\n  gamma  \" ); $words = Mahangu_Troll_Trap::words_from_option(); if ( array( 'alpha', 'beta', 'gamma' ) !== $words ) { echo 'FAIL: words_from_option should trim and drop empty lines, got: ' . var_export( $words, true ) . PHP_EOL; exit( 1 ); } echo 'OK: match_keywords and words_from_option are pure and consistent' . PHP_EOL;"
    },
    {
      "step": "runPHP",
      "code": "<?php require_once '/wordpress/wp-load.php'; update_option( 'trolltrap_words', 'badger' ); update_option( 'trolltrap_default_filter', 'disemvowel' ); update_option( 'trolltrap_graduated_enabled', '0' ); update_option( 'trolltrap_allowed', 'trusted@example.com' ); $tt = mahangu_troll_trap(); $cid = wp_insert_comment( array( 'comment_post_ID' => 1, 'comment_content' => 'badger trash', 'comment_author' => 'Trusted Friend', 'comment_author_email' => 'trusted@example.com' ) ); $tt->comments_tag( $cid ); if ( get_comment_meta( $cid, '_trolltrap_filter', true ) !== 'none' ) { echo 'FAIL: allowlisted author was trapped despite graylist match, got filter: ' . var_export( get_comment_meta( $cid, '_trolltrap_filter', true ), true ) . PHP_EOL; exit( 1 ); } $allowed_hits = get_comment_meta( $cid, '_trolltrap_allowed', true ); if ( ! is_array( $allowed_hits ) || ! in_array( 'trusted@example.com', $allowed_hits, true ) ) { echo 'FAIL: allowlist hit not recorded, got: ' . var_export( $allowed_hits, true ) . PHP_EOL; exit( 1 ); } $cid2 = wp_insert_comment( array( 'comment_post_ID' => 1, 'comment_content' => 'badger trash', 'comment_author' => 'Stranger', 'comment_author_email' => 'stranger@example.com' ) ); $tt->comments_tag( $cid2 ); if ( get_comment_meta( $cid2, '_trolltrap_filter', true ) !== 'disemvowel' ) { echo 'FAIL: non-allowlisted author should still be trapped, got: ' . var_export( get_comment_meta( $cid2, '_trolltrap_filter', true ), true ) . PHP_EOL; exit( 1 ); } $cid3 = wp_insert_comment( array( 'comment_post_ID' => 1, 'comment_content' => 'badger trash, also trusted@example.com is great', 'comment_author' => 'Bypass Attempt', 'comment_author_email' => 'bypass@example.com' ) ); $tt->comments_tag( $cid3 ); if ( get_comment_meta( $cid3, '_trolltrap_filter', true ) !== 'disemvowel' ) { echo 'FAIL: troll bypassed allowlist by quoting trusted email in content body, filter: ' . var_export( get_comment_meta( $cid3, '_trolltrap_filter', true ), true ) . PHP_EOL; exit( 1 ); } update_option( 'trolltrap_allowed', '' ); echo 'OK: allowlist short-circuits for trusted authors but cannot be bypassed by quoting in content' . PHP_EOL;"
    },
    {
      "step": "runPHP",
      "code": "<?php require_once '/wordpress/wp-load.php'; $tt = mahangu_troll_trap(); $cid = wp_insert_comment( array( 'comment_post_ID' => 1, 'comment_content' => 'css class test', 'comment_author' => 'css', 'comment_author_email' => 'css@example.com' ) ); update_comment_meta( $cid, '_trolltrap_filter', 'zalgo' ); $classes = apply_filters( 'comment_class', array( 'comment' ), array(), $cid, get_comment( $cid ), 1 ); if ( ! in_array( 'trolltrap-trapped', $classes, true ) ) { echo 'FAIL: trolltrap-trapped class missing for trapped comment, got: ' . var_export( $classes, true ) . PHP_EOL; exit( 1 ); } if ( ! in_array( 'trolltrap-filter-zalgo', $classes, true ) ) { echo 'FAIL: trolltrap-filter-zalgo class missing, got: ' . var_export( $classes, true ) . PHP_EOL; exit( 1 ); } update_comment_meta( $cid, '_trolltrap_filter', 'none' ); $classes = apply_filters( 'comment_class', array( 'comment' ), array(), $cid, get_comment( $cid ), 1 ); if ( in_array( 'trolltrap-trapped', $classes, true ) ) { echo 'FAIL: untrapped comment should not carry trolltrap-trapped class' . PHP_EOL; exit( 1 ); } update_comment_meta( $cid, '_trolltrap_filter', 'llm' ); update_comment_meta( $cid, '_trolltrap_llm_attempts', 3 ); delete_comment_meta( $cid, '_trolltrap_llm_text' ); $classes = apply_filters( 'comment_class', array( 'comment' ), array(), $cid, get_comment( $cid ), 1 ); if ( ! in_array( 'trolltrap-ai-failed', $classes, true ) ) { echo 'FAIL: trolltrap-ai-failed class missing on a failed AI comment, got: ' . var_export( $classes, true ) . PHP_EOL; exit( 1 ); } echo 'OK: comment_class filter adds trolltrap-trapped, trolltrap-filter-<slug>, and trolltrap-ai-failed' . PHP_EOL;"
    },
    {
      "step": "runPHP",
      "code": "<?php require_once '/wordpress/wp-load.php'; $tt = mahangu_troll_trap(); add_action( 'trolltrap_register_filters', function( $registry ) { $registry->register( 'shout', 'Shout', function( $text ) { return mb_strtoupper( $text ); }, 1 ); } ); do_action( 'trolltrap_register_filters', $tt->filters ); if ( ! $tt->filters->has( 'shout' ) ) { echo 'FAIL: third-party filter registered via action did not land in the registry' . PHP_EOL; exit( 1 ); } if ( 'HELLO WORLD' !== $tt->filters->apply( 'shout', 'hello world' ) ) { echo 'FAIL: registered third-party filter did not transform via apply(), got: ' . var_export( $tt->filters->apply( 'shout', 'hello world' ), true ) . PHP_EOL; exit( 1 ); } if ( ! in_array( 'shout', array_keys( $tt->filters->transforming() ), true ) ) { echo 'FAIL: third-party transforming filter missing from transforming() list' . PHP_EOL; exit( 1 ); } if ( ! in_array( 'shout', array_keys( $tt->filters->enabled() ), true ) ) { echo 'FAIL: third-party filter missing from enabled() list with no disabled-filters option set' . PHP_EOL; exit( 1 ); } echo 'OK: trolltrap_register_filters action accepts third-party registrations end to end' . PHP_EOL;"
    },
    {
      "step": "runPHP",
      "code": "<?php require_once '/wordpress/wp-load.php'; $tt = mahangu_troll_trap(); $cid = wp_insert_comment( array( 'comment_post_ID' => 1, 'comment_content' => 'original text', 'comment_author' => 'edit-test', 'comment_author_email' => 'edit-test@example.com' ) ); update_comment_meta( $cid, '_trolltrap_filter', 'llm' ); update_comment_meta( $cid, '_trolltrap_llm_text', 'rewrite of the original text' ); update_comment_meta( $cid, '_trolltrap_llm_attempts', 2 ); if ( null === $tt->ai->cached_text( $cid ) ) { echo 'FAIL: precondition setup, cached_text should be set before edit' . PHP_EOL; exit( 1 ); } wp_update_comment( array( 'comment_ID' => $cid, 'comment_content' => 'edited content' ) ); if ( null !== $tt->ai->cached_text( $cid ) ) { echo 'FAIL: cached AI rewrite should be cleared after a comment edit, got: ' . var_export( $tt->ai->cached_text( $cid ), true ) . PHP_EOL; exit( 1 ); } if ( '' !== get_comment_meta( $cid, '_trolltrap_llm_attempts', true ) ) { echo 'FAIL: attempts counter should be reset after an edit' . PHP_EOL; exit( 1 ); } $cid2 = wp_insert_comment( array( 'comment_post_ID' => 1, 'comment_content' => 'untrapped', 'comment_author' => 'edit-test-2', 'comment_author_email' => 'edit-test-2@example.com' ) ); update_comment_meta( $cid2, '_trolltrap_filter', 'disemvowel' ); update_comment_meta( $cid2, '_trolltrap_llm_text', 'should-not-touch' ); wp_update_comment( array( 'comment_ID' => $cid2, 'comment_content' => 'still untrapped' ) ); if ( 'should-not-touch' !== get_comment_meta( $cid2, '_trolltrap_llm_text', true ) ) { echo 'FAIL: editing a non-AI-tagged comment should not touch _trolltrap_llm_text' . PHP_EOL; exit( 1 ); } echo 'OK: editing an AI-tagged comment invalidates the cached rewrite; other filters untouched' . PHP_EOL;"
    },
    {
      "step": "runPHP",
      "code": "<?php require_once '/wordpress/wp-load.php'; $tt = mahangu_troll_trap(); update_option( 'trolltrap_disabled_filters', array() ); $all_slugs = array_keys( $tt->filters->transforming() ); $enabled_slugs = array_keys( $tt->filters->enabled() ); if ( $all_slugs !== $enabled_slugs ) { echo 'FAIL: with empty disabled list, enabled() should equal transforming(), got: ' . var_export( array_diff( $all_slugs, $enabled_slugs ), true ) . PHP_EOL; exit( 1 ); } update_option( 'trolltrap_disabled_filters', array( 'zalgo', 'uwu' ) ); $enabled_slugs = array_keys( $tt->filters->enabled() ); if ( in_array( 'zalgo', $enabled_slugs, true ) || in_array( 'uwu', $enabled_slugs, true ) ) { echo 'FAIL: disabled filters should be excluded from enabled(), got: ' . var_export( $enabled_slugs, true ) . PHP_EOL; exit( 1 ); } if ( ! in_array( 'disemvowel', $enabled_slugs, true ) ) { echo 'FAIL: non-disabled filters should still appear in enabled()' . PHP_EOL; exit( 1 ); } $cid = wp_insert_comment( array( 'comment_post_ID' => 1, 'comment_content' => 'historical zalgo comment', 'comment_author' => 'h', 'comment_author_email' => 'h@example.com' ) ); update_comment_meta( $cid, '_trolltrap_filter', 'zalgo' ); $rendered = $tt->filters->apply( 'zalgo', 'test' ); if ( 'test' === $rendered ) { echo 'FAIL: disabled filter should still apply to already-tagged comments, got identity output' . PHP_EOL; exit( 1 ); } update_option( 'trolltrap_disabled_filters', array() ); echo 'OK: enabled() narrows choices but disabled filters still apply to historical comments' . PHP_EOL;"
    }
  ]
}
